{
  "version": 2,
  "buildCommand": "vite build",
  "outputDirectory": "dist",
  "framework": "vite",
  "functions": {
    "api/**": {
      "maxDuration": 0
    }
  },
  "routes": [
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
